[build-system]
requires = ["setuptools>=82"]
build-backend = "setuptools.build_meta"

[project]
name = "comnet"
version = "0.1.0"
dependencies = [
    "requests",
    "pandas",
    "mwparserfromhell",
    "pytest"
]